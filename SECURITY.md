# Security

Do not open public issues containing Telegram bot tokens, chat IDs, Cloudflare
Relay secrets, RAMO_PACKAGE_KEY, or screenshots that expose them.

If any secret is accidentally published, rotate it immediately.
